# prading

An experimental MUD system that develop and extend itself with LLM.

## Bootstrap

The project includes Git submodules that will provide essential coding agent instructions and API contracts.
Make sure the submodule is fetched if you check out a fresh copy of source code:
```
git submodule update --init
```
